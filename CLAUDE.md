This repo is a work in progress implementing the paper on Hybrid Inlining (hybrid-inlining.pdf) in Datalog.

Measuring jobs is expensive but disk space is plentiful. Always capture all output to a file so that
if we need follow up, it's easy to find. You can use `/Volumes/Shampoo` if you need extra storage.
